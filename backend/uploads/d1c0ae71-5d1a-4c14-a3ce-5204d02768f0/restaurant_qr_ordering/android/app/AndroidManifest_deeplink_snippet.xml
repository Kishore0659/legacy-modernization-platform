<!--
  Add this <intent-filter> inside the <activity android:name=".MainActivity">
  block of android/app/src/main/AndroidManifest.xml so the OS routes
  restaurant://menu?table=N links (from a scanned QR code) straight into
  this app.
-->
<intent-filter android:autoVerify="true">
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />
    <data android:scheme="restaurant" android:host="menu" />
</intent-filter>
